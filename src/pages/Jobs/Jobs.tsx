import { LogoutButton } from "../../components/layout/Auth";

const Jobs = () => {
  return (
    <div className="sidebar-bottom mt-auto p-4">
      <LogoutButton type="primary" className="w-full" />
    </div>
  );
};

export default Jobs;
